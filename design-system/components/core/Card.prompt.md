Contêiner padrão de conteúdo: bloco de resposta, painel de fonte, item de inventário.

```jsx
<Card title="Fontes indexadas" meta="59.080 trechos">…</Card>
```

Raio 14px, borda hairline areia, sombra `--shadow-card` baixa. `tone="tinted"` para destaque informativo; `onClick` habilita hover elevado.
