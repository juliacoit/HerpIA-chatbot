Seleção múltipla — filtro de fontes (`fontes: ["monitora","pans","salve"]`).

```jsx
<Checkbox label="SALVE" description="Fichas de espécies" checked onChange={fn} />
```

Quadrado de 18px, raio 4px — o único controle não arredondado do sistema, para distinguir de Radio.
