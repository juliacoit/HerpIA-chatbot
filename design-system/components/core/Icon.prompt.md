Glyph do conjunto Lucide (CDN), usado em todos os controles do HerpIA.

```jsx
<Icon name="search" size="md" />
```

Requer `<script src="https://unpkg.com/lucide@0.454.0/dist/umd/lucide.js">` na página. Tamanhos: sm 14 / md 16 / lg 20 / xl 24; stroke padrão 1.75. Herda cor via `currentColor`.
