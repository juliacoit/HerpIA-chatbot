Ação compacta só com ícone — enviar pergunta, copiar resposta, abrir fonte.

```jsx
<IconButton icon="arrow-up" label="Enviar pergunta" variant="primary" />
```

Sempre circular. `label` é obrigatório. `active` aplica fundo teal claro (estado ligado, ex.: filtro aberto).
