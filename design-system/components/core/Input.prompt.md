Campo de texto de uma linha (busca, número de processo, filtro).

```jsx
<Input label="Buscar na base" iconLeft="search" placeholder="Ex.: anfíbios ameaçados no Cerrado" />
```

Pill com borda hairline; foco adiciona anel teal claro. `error` substitui `hint` e pinta a borda em clay.
