Escolha exclusiva entre 2–4 opções visíveis (ex.: avaliação do feedback, modo de busca).

```jsx
<Radio name="modo" label="Busca semântica" checked />
```

Marcado = borda teal de 5px (sem ponto interno separado).
