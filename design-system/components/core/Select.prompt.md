Escolha única em lista curta — `top_k`, modelo de LLM, ordenação.

```jsx
<Select label="Trechos recuperados (top_k)" options={["3","5","10"]} value="5" />
```

Chevron Lucide à direita; mesma altura e raio do Input.
