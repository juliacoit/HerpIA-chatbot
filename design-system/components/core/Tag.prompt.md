Filtro ou palavra-chave selecionável/removível — táxon, bioma, fonte aplicada à consulta.

```jsx
<Tag selected onClick={fn}>Cerrado</Tag>
<Tag onRemove={fn}>anfíbios</Tag>
```

Difere de Badge: Tag é interativa (seleção/remoção); Badge é só leitura.
