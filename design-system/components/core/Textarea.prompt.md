Texto multilinha — caixa de pergunta longa e comentário de feedback (`comentario`, até 2000 caracteres na API).

```jsx
<Textarea label="Comentário (opcional)" rows={3} hint="Máx. 2000 caracteres" />
```

Raio 14px (não pill, diferente de Input) para sinalizar entrada longa.
