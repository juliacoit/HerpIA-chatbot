Aviso transitório de sistema — falha de serviço (Ollama 503, PostgreSQL indisponível), feedback registrado.

```jsx
<Toast tone="erro" onClose={fn}>LLM local (Ollama) indisponível. Rode "ollama serve".</Toast>
```

Card branco com ícone em disco tonal; nunca use cor de fundo saturada no bloco inteiro.
