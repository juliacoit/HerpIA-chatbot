Faixa de estado acima ou abaixo da resposta, traduzindo `evidencia_suficiente` e `resposta_fundamentada` para o usuário.

```jsx
<EvidenceBanner estado="retida" justificativa="A resposta citou dados não presentes nos trechos recuperados." />
```

Sem borda, só fundo tonal. Mostre `justificativa` sempre que a API a enviar — o projeto exige transparência sobre retenção.
