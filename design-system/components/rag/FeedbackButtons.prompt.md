Avaliação de uma resposta (POST /feedback, `avaliacao: 1 | -1`).

```jsx
<FeedbackButtons value={null} onChange={enviar} />
```

Dois botões circulares de 30px. Selecionado ganha fundo tonal (musgo para positivo, clay para negativo). Comentário opcional vai em `Textarea`.
